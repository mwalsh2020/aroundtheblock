
  <div class="container py-5">
    <h5> Welcome <%= current_user.email %>  </h5>
    <br>
    <div class="row align-items-lg-center">
      <div class="col-8">
        <h2 class="mb-4">List a Project</h2>
        <div>
          <%= simple_form_for @posting, method: :post do |f| %>
            <%= f.input :title, cols: 30, rows: 1 %>
            <%= f.input :price %>
            <%= f.input :description, cols: 80, rows: 5, placeholder: "Describe your awesome project here. We know it's off the chain..." %>
            <%= f.input :photo, as: :file %>
            <%= f.submit %>
          <% end %>
        </div>
       </div>
    </div>
  </div>
  <div class="container py-5">
    <h2>Available Projects</h2>
    <br>
    <% @postings.each do |posting| %>
      <h5><strong><%= link_to posting.title, posting_path(posting) %></strong></h5>
      <p><%= posting.description %></p>
      <p>$<%= posting.price %></p>
      <hr>
    <% end %>
  </div>

